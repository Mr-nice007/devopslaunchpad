/**
 * Dashboard page — PRD 03: Personalized dashboard.
 * Welcome panel (Resume / Unlock), per-module progress, CTAs.
 */
import { DashboardClient } from "./dashboard-client";

export default function DashboardPage() {
  return (
    <main>
      <DashboardClient />
    </main>
  );
} 